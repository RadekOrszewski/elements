{
  "name": "element-trainer",
  "version": "1.0.0",
  "description": "Adaptive periodic table trainer (symbols, names, groups, periods) for Android",
  "scripts": {
    "build:web": "node build-web.js",
    "sync": "node build-web.js && npx cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2",
    "@capacitor/preferences": "^8.0.1"
  },
  "private": true
}